# Verifica que Docker esté instalado correctamente ejecutando una imagen mínima.
# Este contenedor imprime un mensaje y se elimina automáticamente al salir (--rm).

# Comando a ejecutar en la terminal:
# docker run --rm hello-world
